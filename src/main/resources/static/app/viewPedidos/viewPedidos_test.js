'use strict';

describe('myApp.viewPedidos module', function() {

  beforeEach(module('myApp.viewPedidos'));

  describe('viewPedidos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewPedidosCtrl = $controller('ViewPedidosCtrl');
      expect(viewPedidosCtrl).toBeDefined();
    }));

  });
});