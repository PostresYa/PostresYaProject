'use strict';

describe('myApp.viewRegistroPostres module', function() {

  beforeEach(module('myApp.viewRegistroPostres'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewRegistroPostresCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});