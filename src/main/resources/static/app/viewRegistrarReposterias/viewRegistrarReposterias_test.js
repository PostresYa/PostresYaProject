'use strict';

describe('myApp.viewRegistrarReposterias module', function() {

  beforeEach(module('myApp.viewRegistrarReposterias'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewRegistrarReposteriasCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});