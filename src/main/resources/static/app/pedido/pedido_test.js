'use strict';

describe('myApp.pedido module', function() {

  beforeEach(module('myApp.pedido'));

  describe('pedido controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pedidoCtrl = $controller('pedido');
      expect(pedido).toBeDefined();
    }));

  });
});